apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: participant-17

resources:
  - ../../base

patches:
  - target:
      kind: Service
      name: adult-income-api
    patch: |-
      - op: replace
        path: /spec/type
        value: LoadBalancer
